# Content Fixtures

Put public-safe content DB fixture JSONL here.

Use these fixtures for CI and portable tests only. Real imported content and
copyright-sensitive source inputs belong in the nested `data/` repo or in local
generated SQLite files under `server/db/local/`.
