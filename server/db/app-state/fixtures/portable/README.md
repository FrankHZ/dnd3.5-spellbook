# App-State Fixtures

Put public-safe app-state DB fixture JSONL here.

Fixtures should be tiny and synthetic. Long-lived development seed data belongs
in `server/db/app-state/seed.ts` only when it is safe for the parent repo.
