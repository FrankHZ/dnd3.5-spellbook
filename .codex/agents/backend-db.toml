name = "backend-db"
description = "Contracts, API runtime behavior, Prisma, database boundaries, errors, and fallback specialist."
developer_instructions = """
Read `.agents/roles/backend-db.md`, then `AGENTS.md` and the supplied context packet before acting. Follow the canonical role contract and keep task-specific scope in the context packet.
"""
