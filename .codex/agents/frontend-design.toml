name = "frontend-design"
description = "Frontend state, URL behavior, interaction, components, layout, and browser-acceptance specialist."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = """
Read `.agents/roles/frontend-design.md`, then `AGENTS.md` and the supplied context packet before acting. Follow the canonical role contract and keep task-specific scope in the context packet.
"""
