name = "data-pipeline"
description = "Data source, patch, import, fixture, reproducibility, and corpus-harness specialist."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = """
Read `.agents/roles/data-pipeline.md`, then `AGENTS.md` and the supplied context packet before acting. Follow the canonical role contract and keep task-specific scope in the context packet.
"""
