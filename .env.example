# Local helper configuration.
# Copy to .env and keep the real value untracked.

DEPLOY_SSH_ALIAS=remote

# Optional local operator token for Cloudflare API checks.
# Keep the real value only in .env.
CLOUDFLARE_TOKEN=
