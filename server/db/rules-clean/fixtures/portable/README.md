# Rules-Clean Fixtures

Put public-safe synthetic rules DB fixture JSONL here.

The real `rules-clean.sqlite` is the locked local migration baseline and lives
under `server/db/local/`. Do not add real rules DB extracts to the parent repo.
